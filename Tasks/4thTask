Write a C# program that keeps a number from the user and generates an integer between 1 and 7 and displays the name of the weekday. Print "weekend" if the day is part of the weekend or "workday" if otherwise.

using System;
namespace OperatorsAppl
{
    class Program
    {

        static void Main(string[] args)
        {
            int numOfDay = int.Parse(Console.ReadLine());

            if (numOfDay == 1)
            {
                Console.WriteLine("Monday");
            }
            else if (numOfDay == 2)
            {
                Console.WriteLine("Tuesday");
            }
            else if (numOfDay == 3)
            {
                Console.WriteLine("Wednesday");
            }
            else if (numOfDay == 4)
            {
                Console.WriteLine("Thursday");
            }
            else if (numOfDay == 5)
            {
                Console.WriteLine("Friday");
            }
            else if (numOfDay == 6)
            {
                Console.WriteLine("Saturday");
            }
            else if (numOfDay == 7)
            {
                Console.WriteLine("Sunday");
            }
            else
            {
                Console.WriteLine("Error:404");
            }
        }
    }
}
